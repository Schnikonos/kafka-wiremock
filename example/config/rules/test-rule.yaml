priority: 10
name: "test-rule"
when:
  destination: users.commands
  match:
    - type: jsonpath
      expression: "$.eventType"
      value: "USER_CREATED"
then:
  - destination: users.events
    payload: |
      {
        "userId": "{{$.userId}}",
        "event": "USER_CREATED",
        "timestamp": "{{now}}"
      }
